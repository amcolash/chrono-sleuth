let fastMode = false;
fastMode = true;

let debug = false;
debug = true;

let rewindEnabled = false;
rewindEnabled = true;

export const Config = {
  debug,
  width: 1024,
  height: 768,

  dayMinutes: 2,
  rewindEnabled,

  fastMode,
};
